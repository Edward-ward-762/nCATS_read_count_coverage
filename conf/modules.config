process {  
    withName: 'SAMTOOLS_INDEX' {
        publishDir = [
            enabled: false
        ]
    }

    withName: 'SAMTOOLS_COVERAGE' {
        ext.args   = { "-A -r ${meta.referenceName}:${meta.startCutSite}-${meta.endCutSite}" }
        publishDir = [
            path: { "${params.outdir}/SAMTOOLS_COVERAGE" },
            mode: "${params.publish_dir_mode}",
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
        ]
    }

    withName: 'EXTRACT_COVERAGE' {
        publishDir = [
            path: { "${params.outdir}/EXTRACT_COVERAGE" },
            mode: "${params.publish_dir_mode}",
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
        ]
    }

    withName: 'readsCount' {
        publishDir = [
            path: { "${params.outdir}/readsCount" },
            mode: "${params.publish_dir_mode}",
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
        ]
    }

    withName: 'SAMTOOLS_VIEW' {
        ext.args   = { "-h -e 'rlen >= $meta.lengthFilter'" }
        publishDir = [
            path: { "${params.outdir}/SAMTOOLS_VIEW" },
            mode: "${params.publish_dir_mode}",
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
        ]
    }

    withName: 'SAM_INDEX_BAM_FILT' {
        publishDir = [
            enabled: false
        ]
    }

    withName: 'SAM_COV_BAM_FILT' {
        ext.args   = { "-A -r ${meta.referenceName}:${meta.startCutSite}-${meta.endCutSite}" }
        publishDir = [
            path: { "${params.outdir}/SAM_COV_BAM_FILT" },
            mode: "${params.publish_dir_mode}",
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
        ]
    }

    withName: 'extract_bam_filt_count' {
        publishDir = [
            path: { "${params.outdir}/extract_bam_filt_count" },
            mode: "${params.publish_dir_mode}",
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
        ]
    }

    withName: 'bam_filt_cov' {
        publishDir = [
            path: { "${params.outdir}/bam_filt_cov" },
            mode: "${params.publish_dir_mode}",
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
        ]
    }

    withName: 'APPEND_COVERAGE' {
        publishDir = [
            path: { "${params.outdir}/APPEND_COVERAGE" },
            mode: "${params.publish_dir_mode}",
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
        ]
    }

    withName: 'COLLECT_STATS' {
        publishDir = [
            path: { "${params.outdir}/COLLECT_STATS" },
            mode: "${params.publish_dir_mode}",
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
        ]
    }

    withName: 'DUMP_SOFTWARE_VERSIONS' {
        publishDir = [
            path: { "${params.outdir}/pipeline_info" },
            mode: "${params.publish_dir_mode}",
            pattern: '*_versions.yml'
        ]
    }
}